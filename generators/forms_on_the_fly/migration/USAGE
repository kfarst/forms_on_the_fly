Forms on the Fly
========

# Generate migrations for storing user-created forms, user-created form fields, and form input values
# It generate three migrations for "", "", and "" tables

rails generate forms_on_the_fly:migration

# options:
  --backend, [--backend=PROCESSOR] # Configure for selected file uploader (options: paperclip/attachment_fu)
